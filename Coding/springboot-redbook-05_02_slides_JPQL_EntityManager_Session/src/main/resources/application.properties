# H2 Database Configuration (for local development)
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Enable H2 Console (accessible at http://localhost:8080/h2-console)
spring.h2.console.enabled=true

# JPA/Hibernate Properties
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Initialize data after schema creation
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always

# Uncomment below for MySQL when available
# spring.datasource.url=jdbc:mysql://192.168.5.13:3306/redbook?useSSL=false&serverTimezone=UTC
# spring.datasource.username=chuwa
# spring.datasource.password=chuwa
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5InnoDBDialect