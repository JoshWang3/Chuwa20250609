docker build -t springboot-redbook-app:dev .
kubectl apply -f mysql-deployment.yaml
kubectl apply -f dev-deployment.yaml
kubectl get pods -w
kubectl get svc

# Then open in browser:
http://localhost:30080

# For Log debug:
kubectl logs -l app=redbook --tail=100 -f