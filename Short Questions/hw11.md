## Question 3
Model mappers in Spring are needed to convert between different object types, for security and flexibility.
- API Layer: Convert between DTOs and entities when handling REST requests/responses
- Database Operations: Convert between entities and domain objects

## Question 4
1. Incompatible types 
```java
public class OrderDTO {
    private String createdDate = "2024-01-15T10:30:00Z";
    private String status = "PENDING";
}
public class OrderEntity {
    private LocalDateTime createdDate;
    private OrderStatus status;
}
```
2. Circular reference
```java
public class Department {
    private String name;
    private List<Employee> employees;
}

public class Employee {
    private String name;
    private Department department; 
}
```

3. Name mismatch

```java
public class OrderDTO {
    private String createdDate = "2024-01-15T10:30:00Z";
    private String status = "PENDING";
}
public class OrderEntity {
    private String date;
    private String time;
    private String status;
}
```

## Question 5
Automatic:
-   String to Number 
-   String to Boolean
-   Number type promotions (int → long, float → double)
-   Date/Time conversions

Customized: 
- e.g. @Mapping(target =  "birthDate", source =  "birthDateString")

Enum:
- e.g.
```java
// Source
public class UserDto {
    private String status; // "ACTIVE"
}
public class User {
    private UserStatus status;
}
public enum UserStatus {
    ACTIVE, INACTIVE, PENDING
}
```

## Question 7
Controller Advice uses Aspect-Oriented Programming to intercept exceptions thrown by controllers globally. Spring creates a proxy around controllers, When an exception occurs, the DispatcherServlet catches it, HandlerExceptionResolver chain then processes the exception. ExceptionHandler finds matching @ExceptionHandler methods, thus invoke appropriate handler method.

Other approach:
Custom ResponseEntityExceptionHandler - 
```java
@ControllerAdvice
public class CustomExceptionHandler extends ResponseEntityExceptionHandler {
    
    @Override
    protected ResponseEntity<Object> handleMethodArgumentNotValid(
            MethodArgumentNotValidException ex, HttpHeaders headers, 
            HttpStatus status, WebRequest request) {
        return ResponseEntity.badRequest().body(createErrorResponse(ex));
    }
}
```

## Question 8
![demo_1](demo_1.png)
![demo_2](demo_2.png)
Regular Exception: Service → throws RuntimeException → GlobalExceptionHandler → Generic 500 response
Custom API Exception: Service → throws BlogAPIException → GlobalExceptionHandler → Specific HTTP status + message

## Question 9
![demo_3](demo_3.png)
## Question 10
1. Inversion of Control: Spring manages and injects objects' dependencies
2. Dependency Injection: Spring automatically provides dependencies to objects
3. Aspect-Oriented Programming: Separates cross-cutting concerns (logging, security, transactions) from business logic
4. Bean Lifecycle Management: Spring manages object creation, initialization, and destruction through its container

Scalability and performance: Built-in caching etc. handled automatically
Maintainable architecture: Through loose coupling 
Separate of concern: Through AOP
Rapid Development

## Question 11
1. Constructor Injection: Dependencies provided through the class constructor
- Immutable objects
- Required dependencies
- Core application components
```java
@Service
public class OrderService {
    private final PaymentService paymentService;
    public OrderService(PaymentService paymentService) {
        this.paymentService = paymentService;
    }
}
```
2. Setter Injection: Dependencies setter methods
- Optional dependencies
- Configuration properties
```java
@Service
public class OrderService {
    private PaymentService paymentService;
    @Autowired
    public void PaymentService(PaymentService paymentService) {
        this.paymentService= paymentService;
    }
}
```
3. Field Injection: Dependencies provided by injecting directly into fields using @Autowired
- Not recommended, because:
- - hard to test
- - dependencies invisible to clients
- - mutable dependencies
```java
@Service
public class OrderService {
    @Autowired
    private PaymentService paymentService;
```
## Question 12
ApplicationContext:
- ClassPathXmlApplicationContext: load from XML
- FileSystemXmlApplicationContext: load from XML, use absolute path
- AnnotationConfigApplicationContext: from Java classes with annotations

WebApplicationContext - Root context for web applications

SpringApplication - Spring Boot's enhanced application context:
- ReactiveApp: For reactive/WebFlux applications

## Question 13

@Component - Used to mark a class as a Spring-managed component for automatic detection and registration.
- Class declarations

@Bean - Used to declare a bean explicitly in a configuration class, giving you full control over instantiation.
- Method declarations inside @Configuration classes

## Question 3
![demo_4](demo_4.png)
## Question 4
![demo_5](demo_5.png)
