microservice repo: Create a new service called  AgentService and register it to Euraka server and API gateway. use open feign  in AgentService to make calls to both CitizenService and VaccinationService (any endpoint is fine). (create your database record as needed)
Share your screen shot in markdown file.
example code will be provided after 07/28 in feign-client branch. try without looking at this first.

![Alt text](/Short%20Questions/img/hw17_citizenService.png)

![Alt text](/Short%20Questions/img/hw17_VaccinationService.png)


kafka repo: create your database/DAO and save the message in the  consumer. implement both At-Least-Once and At-Most-Once.  
share your screen shot in the markdown file


**At-Least-Once**
```java
 @KafkaListener(topics = "${kafka.topic.name}", groupId = "${spring.kafka.consumer.group-id}", containerFactory = "manualAckFactory")
    public void listenGroupFoo(ConsumerRecord<String, String> record, @Header(KafkaHeaders.ACKNOWLEDGMENT) Acknowledgment ack) {
        //System.out.println("Received message: " + message + " from group: " + consumerGroupId + " with topic: " + topic);
        System.out.printf("Received message: [key = %s, value = %s]\n", record.key(), record.value());
        if (record.key() == null || record.key().isEmpty() || record.value() == null || record.value().isEmpty()) {
            throw new IllegalArgumentException("Key and value cannot be null or empty");
        }

        try{
            messageService.save(record.key(), record.value());
            ack.acknowledge();
        }catch(Exception e){
            System.err.println("Failed to process message:" + e.getMessage());
        }

        // {"messageKey":1234 //idempotency key
        // }
//        try {
//            //process and persist message
//        } catch (Exception e) {
//            //send to dead letter queue
//        }
        //commit to offset here
        //return
    }

```
```java

    @KafkaListener(topics = "chuwa-yyds", groupId = "consumer_group_2", containerFactory = "kafkaListenerContainerFactoryGroup2")
//    @KafkaListener(topics = "chuwa-yyds", groupId = "consumer_group_2", containerFactory = "manualActFactory")

    public void consumerGroup2(ConsumerRecord<String, String> record, @Header(KafkaHeaders.ACKNOWLEDGMENT) Acknowledgment ack){
    //    System.out.println("Received message: " + message + " from group: " + "consumer_group_2" + " with topic: " + topic);
        //System.out.printf("At-Most-Once: Received message: [key = %s, value = %s]\n", record.key(), record.value());
        try{
            //1. Commit FIRST
            ack.acknowledge();

            // 2. Process AFTER commit
            System.out.printf("At-Most-Once: Received message: [key = %s, value = %s]\n", record.key(), record.value());
            messageService.save(record.key(), record.value());
        }catch(Exception e){
            System.err.println("Message lost due to: " + e.getMessage());
        }
    }
```

For At Least Once:
![Alt text](/Short%20Questions/img/hw17_atLeastOnce_erroroccur.png)
After fix the problem, the consumer consume all messages
![Alt text](/Short%20Questions/img/hw17_atLeastOnce_errorfixed.png)

When run both consumer as at-Least-Once and at-Most-Once:
![Alt text](/Short%20Questions/img/hw17_erroroccur.png)
After fixing the problem, the At-Least-Once will complete the consumption
![Alt text](/Short%20Questions/img/hw17_errorFix.png)