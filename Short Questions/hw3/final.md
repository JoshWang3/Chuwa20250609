The final keyword is a non-access modifier used to enforce restrictions on variables, methods, and classes.
1. Field: Declaring a variable as final makes it a constant, meaning its value cannot be changed once it's initialized and can only be initialized once.
2. Method: Declaring a method as final prevents it from being overridden by subclasses.
3. Declaring a class as final prevents it from being subclassed (inherited).