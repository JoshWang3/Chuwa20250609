Spring Data JPA: High-level implementation for JPA-based data access with ORM layer (Hibernate).

Hibernate: JPA provider that handles object-relational mapping and SQL generation(JDBC internal).

JDBC: Low-level API that executes SQL commands and communicates directly with the database.

HikariCP: Connection pool that manages and optimizes JDBC connections for performance.